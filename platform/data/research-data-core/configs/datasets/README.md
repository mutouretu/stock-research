# Dataset definitions

Each YAML file defines one dataset. Paths are normally relative to the `stock-research` root.
`columns` maps canonical names to physical source columns; entity and time semantics are explicitly
declared instead of inferred from particular market fields. Files under `examples/` are contracts
and may point to data that is not present locally.
