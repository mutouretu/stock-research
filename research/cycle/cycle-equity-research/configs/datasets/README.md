# CF dataset contracts

These YAML files can be parsed by `research_data_core.data.DatasetConfig`. They describe the
expected storage layout and source fields but do not assert that physical files already exist.
