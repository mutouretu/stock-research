# Config Layout

- `common/`: generic baseline dataset, train, inference, and review configs.
- `type_n/`: Type-N strategy configs, including phase1 breakout and phase2 pullback.
- `new_high/`: new-high strategy configs.
- `local_runs/`: ad hoc local run configs kept for reproducibility.
- `archive/`: historical configs that are no longer the active default.

