# Reviewer Recycle Bin

This directory keeps legacy reviewer compatibility shims that are no longer part
of the active import tree. Active strategy reviewers live under strategy-specific
directories such as `src/reviewers/type_n/`.

